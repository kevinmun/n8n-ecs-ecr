# Use the official n8n image
FROM docker.n8n.io/n8nio/n8n:latest

# n8n runs on port 5678 by default
EXPOSE 5678

# The n8n image already has everything configured
# Data will be persisted via EFS mount at /home/node/.n8n
